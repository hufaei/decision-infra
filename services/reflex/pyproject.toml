[project]
name = "reflex-container-layer"
version = "0.1.0"
description = "Container and run notes for the upstream Reflex System One server"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
